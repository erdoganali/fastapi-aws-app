# fastapi-aws-app

terraform init
terraform validate
ssh-keygen
<Enter>
<Enter>
sudo mv ~/.ssh/id_rsa.pub ~/.ssh/mlops-aws.pub

terraform validate
terraform plan 
terraform apply --auto-approve
terraform destroy --auto-approve
  
